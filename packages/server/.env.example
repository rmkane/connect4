# Copy to `.env` in this directory (or export vars in your shell). Loaded via `dotenv` in dev/prod entry.

PORT=3000
NODE_ENV=development
LOG_LEVEL=debug

# Optional: cap incoming WS message size (bytes)
# MAX_WS_MESSAGE_BYTES=16384
